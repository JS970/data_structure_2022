자료구조 과제 해야할 것

GetData() 함수 난수를 입력받아 사용하고 화면에서 입력받지 않는 버전으로 수정 (Done)
-> degree가 4라면 지수는 x^4부터 내림차순으로 자동 생성된다. 계수의 경우 난수 생성하여 정수 변환하여 자동 입력되도록 한다.

NewTerm(const float theCoeff, const int theExp) 동작 원리 및 코드 이해, memcpy구현(copy -> memcpy) (Done)
-> memcpy -> (복사 받을 메모리, 복사 해줄 메모리, 복사할 크기)

operator overloading 구현
+(Done), -(Done), *(Done), <<(Done), >>(Done)

Polynomial Substract(Polynomial a, Polynomial b); 구현(Done)

Polynomial Mult(Polynomial b); 구현(Done)

float Eval(float f); 구현(Done)

main함수 다듬기 (Done)